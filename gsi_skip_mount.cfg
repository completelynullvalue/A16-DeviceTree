# GrapheneOS GSI Clean skip mount configuration
# This file tells the system to skip mounting certain partitions
# that are not needed for GSI builds

# Skip mounting product partition (handled by GSI)
/product

# Skip mounting system_ext partition (handled by GSI)  
/system_ext

# Skip mounting vendor partition (handled by GSI)
/vendor











